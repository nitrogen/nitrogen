[

{title, "Value Element"},

{see_also, [base, label, span]},

{usage, "
	#value { text=\"Some text.\" }
"},

{description, "The value element produces an HTML span with a CSS type of 'value'."},

{attributes, [

	{"text", "string", "The text to display."},
	
	{"html_encode", "boolean", "Set to true to safely html-encode the displayed value."}
	
]}

].