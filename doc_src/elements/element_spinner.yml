title: Spinner Element
see_also:   base

description: >
  The spinner element places an AJAX spinner on the page.
  <br>
  The spinner is automatically displayed during a postback, 
  and hidden when the postback completes.

  
attributes:
  - name: image
    type: string
    description: The spinner image to display. Defaults to "/nitrogen/spinner.gif".